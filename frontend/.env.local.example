# =====================================================
# .env.local.example — frontend env template
# =====================================================
# שימוש: cp .env.local.example .env.local  (לא נכנס ל-git)
#
# הערכים מתחילים ב-NEXT_PUBLIC_ → נחשפים לדפדפן (it's intentional —
# Supabase publishable key מיועד להיות בצד-לקוח).
# =====================================================

NEXT_PUBLIC_SUPABASE_URL=https://bfnmaogcxdgnaxwjdtny.supabase.co
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=sb_publishable_7RMhqEoPZs73M1ZSbP0Uww_YLE9Nv76

# Backend FastAPI URL — לוקאלי כברירת מחדל, פרוד אחרי פריסה ל-Render
NEXT_PUBLIC_API_URL=http://localhost:8000
